apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bruh.fullname" . }}-test-connection"
  labels:
    {{- include "bruh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bruh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
