apiVersion: v1
kind: Service
metadata:
  name: {{ include "bruh.fullname" . }}
  labels:
    {{- include "bruh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bruh.selectorLabels" . | nindent 4 }}
